services:
  frontend:
    container_name: frontend
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - 80:80
    networks:
      - gdpr-network

networks:
  gdpr-network:
    external: true
